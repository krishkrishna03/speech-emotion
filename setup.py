from setuptools import setup, find_packages

setup(
    name="speech-emotion",
    version="1.0.0",
    description="Speech Emotion Recognition Model",
    packages=find_packages(),
    python_requires=">=3.10",
    include_package_data=True,
)
