services:
  - type: web
    name: speech-emotion
    runtime: python
    runtimeVersion: "3.10.13"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.10.13"
